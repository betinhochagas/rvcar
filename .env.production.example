# Exemplo de configuração para PRODUÇÃO
# Copie para .env.production e ajuste com suas URLs

# URL da API em produção
# Use URL relativa se a API estiver no mesmo domínio
VITE_API_URL=/api/vehicles.php
VITE_USE_API=true

# OU se a API estiver em domínio diferente:
# VITE_API_URL=https://api.seusite.com.br/vehicles.php
# VITE_USE_API=true

# Para deploy em subpasta (ex: seusite.com.br/rvcar/)
# VITE_BASE_URL=/rvcar/
