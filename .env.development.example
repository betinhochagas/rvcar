# Exemplo de configuração para DESENVOLVIMENTO LOCAL
# Copie para .env.local e ajuste conforme necessário

# Exemplo de configuração para DESENVOLVIMENTO LOCAL
# Copie para .env.local e ajuste conforme necessário

# OPÇÃO 1: Detecção Automática (RECOMENDADO)
# Deixe vazio e o sistema detecta automaticamente o ambiente
VITE_API_URL=
VITE_USE_API=true

# OPÇÃO 2: Forçar URL Específica (apenas se necessário)
# Descomente apenas se a detecção automática não funcionar
# VITE_API_URL=http://localhost:3000/api
# VITE_USE_API=true

# OPÇÃO 3: Modo Offline (sem API, usa localStorage)
# VITE_USE_API=false
